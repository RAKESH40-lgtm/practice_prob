/**
 * Problem: Prg to print the elements in a array
 * Algorithm:
 * step1:Declare and initialize an array
 * step2:use for loop to traverse inside array with index value as 0.
 * step3: print the array
 */
//code.
let arr=[1,2,3,4,5];
for(let i=0;i<arr.length;i++){
    console.log(arr[i]);
}
console.log(arr)
/**
 * Dry Run:
 * line9:create arr of elements [1,2,3,4,5]
 * line10-11:inside loop at index start from 0
 *         i=0 arr 1... so on
 * line13:print the entire arr
 */